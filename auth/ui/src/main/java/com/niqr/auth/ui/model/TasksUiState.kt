package com.niqr.auth.ui.model

import com.niqr.auth.ui.AuthScreen

/**
 * Main state of [AuthScreen]
 *
 * (There is no state yet)
 */
data class AuthUiState(
    val state: String = "There is no state yet"
)
